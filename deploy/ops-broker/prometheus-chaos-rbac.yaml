apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ops-broker-chaos
  namespace: prometheus-system
rules:
  - apiGroups: ["chaos-mesh.org"]
    resources: ["podchaos", "networkchaos"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]    # /v1/logs reads Prometheus pods (default log allowlist)
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ops-broker-chaos
  namespace: prometheus-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ops-broker-chaos
subjects:
  - kind: ServiceAccount
    name: ops-broker
    namespace: opencost
